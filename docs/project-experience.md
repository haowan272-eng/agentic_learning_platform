# 项目经历

## Agentic Learning RAG | 多智能体面试学习平台

- 设计多智能体任务的可重放执行链路：以请求幂等键和事务 Outbox 保证任务创建一致性，以任务租约避免并发 Worker 重复执行，并用稳定的 `task_id + call_id` 持久化、回放已完成工具结果；约定写操作将同一幂等键透传至下游，降低网络重试和 Worker 恢复场景下的重复任务与重复副作用风险。
