# Helm Deployment

Kubernetes and Helm deployment assets belong here.

Suggested layout:

```text
deploy/helm/
|-- Chart.yaml
|-- values.yaml
`-- templates/
```

The current Docker Compose deployment lives under `docker/`.
